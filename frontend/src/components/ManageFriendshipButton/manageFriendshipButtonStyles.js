import {makeStyles} from '@material-ui/core/styles'

const useStyles = makeStyles(() => ({
    button: {
        position: 'absolute',
        padding: '2px 4px',
        bottom: '5px',
        right: '5px'
    }
}))

export default useStyles
